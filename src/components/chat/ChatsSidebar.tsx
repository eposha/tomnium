import {ProfileCard} from '@/components/common/profile';

import * as UI from 'styles/chat/sidebar';
const ChatsSidebar = () => {
  return (
    <UI.Sidebar>
      <ProfileCard />
    </UI.Sidebar>
  );
};

export default ChatsSidebar;
