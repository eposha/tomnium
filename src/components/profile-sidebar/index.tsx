import {ProfileCard} from '@/components/common/profile';

import * as UI from 'styles/profile-sidebar';
const ProfileSidebar = () => {
  return (
    <UI.Sidebar>
      <ProfileCard />
    </UI.Sidebar>
  );
};

export default ProfileSidebar;
