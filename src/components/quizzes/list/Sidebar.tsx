import {ProfileCard} from '@/components/common/profile';

import * as UI from 'styles/quizzes/list/sidebar';

const QuizListSidebar = () => (
  <UI.Sidebar>
    <ProfileCard />
  </UI.Sidebar>
);

export default QuizListSidebar;
